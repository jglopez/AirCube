# AirCube macOS development dependencies
# Usage: brew bundle

tap "espressif/eim"

brew "cmake"
brew "dfu-util"
brew "python" # ESP-IDF requires >= 3.10; resolves to current Homebrew default
brew "eim"
brew "esptool" # firmware flashing utility
